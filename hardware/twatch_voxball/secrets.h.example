#pragma once
// Copy this file to "secrets.h" (same folder) and fill in your network to enable
// wireless OTA updates (see ota.h / the README's "Wireless OTA updates" section).
// secrets.h is gitignored — never commit real credentials.
#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

1. **Fix missing `aria-pressed` on toggle buttons:**
   - Update `#cbToggle`, `#pauseCanvasBtn`, and `#motionToggle` in `app.js` to set `aria-pressed` based on their state to convey state to screen readers.
2. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
3. **Submit the change.**
   - Include branch name, title, and description.

{
  "name": "prosody-ball",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:conflicts": "node check-conflicts.mjs",
    "start": "npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
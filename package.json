{
  "name": "vox-ball",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "check:conflicts": "node check-conflicts.mjs",
    "start": "npx serve .",
    "hue": "node hue-bridge.mjs",
    "test:audio-fixtures": "node tools/eval-audio-fixtures.mjs",
    "eval:formants": "node tools/eval-formant-methods.mjs",
    "test:all": "npm run lint && npm run test:unit && npm run test:audio-fixtures",
    "test:unit": "node --test *.test.mjs",
    "test:browser-smoke": "node tools/browser-smoke.mjs",
    "test:browser-matrix": "npm run test:browser-smoke && BROWSER=firefox npm run test:browser-smoke",
    "syntax-check": "node --check app.js && node --check voice-analyzer.js && node --check dsp-utils.js && node --check calibration-wizard.js && node --check bulb-controller.js && node --check necklace-controller.js && node --check performance-monitor.js && node --check hue-bridge.mjs && node --check tools/browser-smoke.mjs",
    "lint": "npm run syntax-check && eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "npm run lint"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "prettier": "^3.8.4",
    "puppeteer": "^24.40.0",
    "serve": "^14.2.0"
  }
}
